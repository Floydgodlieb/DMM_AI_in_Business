# Instructions for the CLI assistant (rename to CLAUDE.md / AGENTS.md)

Read `context.md` before every task. It is the only source of the target reader, the six quality criteria, the out-of-scope list and the house style. Do not keep your own copy of any of it.

Rules that hold for every stage:
- Work only with files inside this `platform/` folder. Never read, request or reference any recording, transcript or file outside it. Interview material is entered by a person; you never process it.
- Every fact you write down carries a source id and the verbatim sentence it rests on. If you cannot point to a sentence in `sources/`, write nothing.
- Never edit `original_nl`. Never fill `verified_by`; a person does.
- Never change a rating a person has overridden.
- Never publish. Stage 6 is done by a person.
- Output only the file the stage asks for, in the exact shape of its template. No prose around it.
